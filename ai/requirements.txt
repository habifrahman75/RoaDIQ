# RoadIQ AI Service – requirements.txt
# Install with:  pip install -r requirements.txt
#
# Run from the ai/ directory:
#   uvicorn main:app --host 0.0.0.0 --port 8001 --reload

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── HTTP client (used by verification.py to fetch after-repair images) ───────
httpx==0.27.0

# ── Image processing ─────────────────────────────────────────────────────────
Pillow>=10.4.0

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Form / multipart (required by FastAPI File() uploads) ────────────────────
python-multipart==0.0.9

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0

# ── Real AI inference (OPTIONAL) ──────────────────────────────────────────────
# Uncomment both lines below when a trained YOLO model is available.
# Requires Python ≥ 3.8 and a compatible PyTorch installation.
# See: https://docs.ultralytics.com/quickstart/
#
# ultralytics==8.2.0
#
# GPU support (install BEFORE ultralytics):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# CPU-only PyTorch (slower but works on any machine):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
